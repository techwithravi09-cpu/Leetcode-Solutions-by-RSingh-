        n=len(nums)
        ans=[-1]*n
        stack=[]
        for i in range(2*n-1,-1,-1):
            while len(stack)!=0  and nums[stack[-1]]<=nums[i%n]:
                stack.pop()
            if i<n:
                if len(stack)!=0:
                    ans[i]=nums[stack[-1]]
            stack.append(i%n)
        return ans
